server {
        listen *:80;
        listen [::]:80;
        server_name {{ vhost1_Name }};

        root        /var/www/{{ vhost1_Name }}/html;

        location / {
                try_files $uri $uri/ =404;
        }
}
